// re-exported from services/segmentClassifier — import directly from there for new code
export { classifyEntity, classifyServices, classifyComplianceRisk, classify } from '../services/segmentClassifier'
export type { EntitySegment, ServiceSegment, ComplianceRisk } from '../types'
