# ARC Onboarding

ARC Onboarding 프로토타입과 기준 문서를 관리하는 저장소다.

## 구조

- `docs/`
  - PRD, 화면 정의, 맥락 요약 등 기준 문서
- `prototype/`
  - GitHub Pages로 바로 호스팅할 정적 프로토타입

## 배포

정적 프로토타입은 `prototype/` 기준으로 관리한다.
GitHub Pages를 사용할 경우, 이 폴더를 배포 대상으로 지정하면 된다.
